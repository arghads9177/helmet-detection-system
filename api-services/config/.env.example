# api-services/config/.env.example — committed; documents every variable with a safe default.
# Copy to api-services/config/.env (gitignored) and fill in real values.

API_HOST=0.0.0.0
API_PORT=8000
CAMERA_URL=
LOG_LEVEL=INFO
