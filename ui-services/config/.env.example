# ui-services/config/.env.example — committed; documents every variable with a safe default.
# Copy to ui-services/config/.env (gitignored) and fill in real values.

API_BASE_URL=http://localhost:8000
UI_PORT=8501
